## Morning TODO

1. github.com -> New Repo (Repo name: second - repo ( README.md MIT LICENSE))
2. 내 컴퓨터에 clone(~/Documents/dev/)
3. touch .gitignore -> Setting(내 환경에 맞춰서, python, vim, OS from https://gitignore.io)
4. README.md -> 기본구조
5. touch main.py -> print('hello')

## How to use pre-commit

1. $ pip install pre-commit
2. $ pre-commit --v 로 pre-commit 명령어 인식유무 확인
3. $ pre-commit sample-config > .pre-commit-config.yaml
4. $ pre-commit install 하여 commit 시 자동실행하기 (강제실행은 $ pre-commit run)
5. $ 작업 -> add, commit -> pre-commit 동작 및 자동수정 -> 수정사항 add, commit -> 완료

https://pre-commit.com/hooks.html 추가 방법

-   repo: https://github.com/psf/black
    rev: stable
    hooks:
    - id: black
      language_version: python3.14.2
-   repo: https://github.com/PyCQA/flake8
    rev: 7.0.0
    hooks:
    - id: flake8

## Mini DIY

1. New branch fizzbuzz
2. Switch to branch fizzbuzz
3. 15의 배수일 때, fizzbuzz를 출력하는 코드 추가

for i in range(1, 15+1):
  if i % 15 == 0:
    print('fizzbuzz')

4. add, commit
5. Switch to branch main
6. Merge branch 'fizzbuzz'
7. Delete branch 'fizzbuzz'

## Merge Conflict

$ git branch buzz
(main) -> fizz 구현 -> add, commit

$ git switch buzz
(buzz) -> buzz 구현 -> add, commit

$ git switch main
$ git merge buzz -> merge conflict 해결 -> add, commit

## Merge Conflict(2)

1. $ git branch fb-refactor
2. (main) -> fizzbuzz 구현 -> add, commit
3. $ git swotch fb-refactor
4 (fb-refactor) -> fizzbuzz(1개 if문) 구현 -> add, commit
5. $ git switch main
6. $ git merge fb-refactor -> merge conflict 해결( fb-refactor 코드 채택 ) -> add, commit

for i in range(1, 15+1):
  if i % 3 == 0 or i % 5 == 0"
    print('fizz'*(i%3==0) + 'bizz'*(i%5==0))
  else:
    print(i)

## github flow

github
  1. 새 issue를 생성하고 할 일에 대한 세부정보 작성(설계)

Local
  1. 작업할 새 브런치 작성
  2. 새 브런치 위에서 작업 실시
  3. git push origin {새 브랜치}

github
  1. Comapare * Pull Request 버튼을 눌러서 새 PR(Pull Request) 생성
  2. 1의 이슈번호와 연결(fix,close,resolve+{이슈번호})
  3. 내용 확인 후 Merge Pull Request 버튼 누르기
  4. Delete Branch

Local
  1. $ git swtich main
  2. main branch 업데이트 하기($ git fetch origin main, $ git merge FETCH_HEAD)
  